/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/vendor
.env
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode

# Large downloaded assets (OCR language data, dictionary) — fetched by setup, not committed
/storage/tessdata/*.traineddata
/storage/dict/*.u8
/storage/dict/*_index.php

# Database snapshots pulled from production. Real user data — never committed.
backups/

# Scratch from manual API testing — a booking payload and its id.
bk.json
bkid.txt
